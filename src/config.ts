export const UNSPLASH_ACCESS_KEY = import.meta.env.VITE_UNSPLASH_ACCESS_KEY || "";
// Activate mock by set this env variable as true
export const MOCK = import.meta.env.VITE_MOCK === "true";

// Constants values, change if needed
export const UNSPLASH_URL = "https://api.unsplash.com";
export const REQUEST_TIMEOUT = 5000;
export const ROWS_PER_PAGE = 24;
