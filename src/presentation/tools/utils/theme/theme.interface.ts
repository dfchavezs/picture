export interface ITheme {
  header: {
    bgColor: string;
    searchTextColor: string;
  };
  body: {
    bgColor: string;
    titleColor: string;
  };
  photo: {
    bgColor: string;
  };
  pagination: {
    selectedBg: string;
    selectedTextColor: string;
    textColor: string;
    textColorHover: string;
  };
  maintenance: {
    textColor: string;
  };
}
